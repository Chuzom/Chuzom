# awesome-ai-plugins submission (issue #103)

Chuzom already ships a Codex plugin manifest (`.codex-plugin/plugin.json`), so it
qualifies for the **awesome-ai-plugins** list. Listed projects also surface in HOL's
plugin directory: https://hol.org/registry/plugins

## Ready-to-submit entry

Add this line to the appropriate category in the awesome-ai-plugins `README.md`
(match that repo's `CONTRIBUTING.md` format), then open a PR **there**:

```markdown
- [Chuzom](https://github.com/Chuzom/Chuzom) — Routes every Codex/Claude task to the cheapest capable model (Gemini Flash, Haiku, Ollama, Perplexity, 20+ providers); tracks cross-session savings and enforces routing policy.
```

Suggested category: **Developer Tools** (matches `plugin.json` → `interface.category`).

## Note

Submitting the PR to the external `awesome-ai-plugins` repository requires your own
GitHub account and a fork of that repo — it can't be done from this workspace. This
file captures the entry so the submission is a copy-paste away.
