# Release-Hardening Implementation Evidence

Compact before/after record per finding. Populated incrementally, one row-set
per cluster commit, in the dependency order defined by
`REMEDIATION_PLAN.md` / `REMEDIATION_MATRIX.json`.

| id | cluster | test added | before (fail reason) | after (pass) | sibling paths fixed | commit |
|----|---------|-----------|----------------------|--------------|----------------------|--------|
| C-01 | 6 — Concurrent persistence | `tests/test_session_store_concurrency.py::test_session_store_concurrency_zero_lost_writes` | Ran against pre-fix `session_store.py` (via `git stash`): `AssertionError: round 0: 26 writes were lost (first few: ['MARK-w0-i65-pid72752', 'MARK-w0-i91-pid72752', 'MARK-w0-i119-pid72752', 'MARK-w0-i120-pid72752', 'MARK-w0-i175-pid72752', 'MARK-w1-i60-pid72753', ...])` — 26/1200 (≈2.2%) lost writes across 6 real processes × 200 events, consistent in magnitude with the original audit's 22/1200 (1.83%) finding for CHZ-AUD-C-01. Root cause: `record_event()`'s append-then-maybe-compact critical section had no cross-process coordination — a concurrent process's compaction could read a stale file snapshot (taken before another process's append landed) and `os.replace()` the file with that stale snapshot, silently orphaning the just-written record. | Fixed via `src/chuzom/file_lock.py` (new cross-platform advisory exclusive lock: POSIX `fcntl.flock`, Windows `msvcrt.locking`, sibling `.lock` file so the data file's inode can be freely swapped by compaction's `os.replace()` without disturbing lock identity) wrapping the entire append+dedupe-check+maybe-compact critical section in `session_store.record_event()`. 0 lost writes across: the initial run (3 rounds × 6 workers × 200 events = 3600 writes), 3 additional standalone repeat runs (no flakiness), and the full adjacent/sibling sweep — `151 passed` across `tests/test_session_store.py`, `tests/test_session_store_concurrency.py`, `tests/test_context.py`, `tests/test_context_capture_hook.py`, `tests/test_session_context_wiring.py`, `tests/test_session_end_context_archive.py`, `tests/test_session_last_activity.py`, `tests/test_session_start_context_pointer.py`, `tests/test_sec01_shared_scrubber.py`, `tests/test_red2_04_privacy_local_blocks_all_external.py`, `tests/test_chz_aud_022.py`, `tests/test_chz_aud_023.py`, `tests/test_chz_aud_024.py`. | None found. Searched `src/chuzom` for the same shape (periodic full-file-rewrite-via-compaction combined with concurrent multi-process appends to the same file): `grep -rn "os.replace\|mkstemp" src/chuzom --include="*.py"` (13 files — all either SQLite-based (`cost.py`) or simple atomic single-file writes without a compaction-rewrite race) and `grep -rln "\.jsonl\|_maybe_compact\|compact_to\|COMPACT_TO" src/chuzom --include="*.py"` (~24 JSONL-referencing files) narrowed to the closest JSONL-log-like candidates — `lineage/lineage_store.py`, `library/sealer.py`, `hooks/lineage_integration.py`, `hooks/savings_logger.py`, `model_tracking.py` — none of which use `os.replace`/`mkstemp` (`grep -rn "os.replace\|mkstemp" <those 5 files>` → no output), i.e. none perform compaction-style full-file rewrites. The torn-compaction race pattern is unique to `session_store.py` in this codebase. | `0a87dd4` |
| B-04 | 2 — Scope identity | `tests/test_session_buffer_scope.py` (13 tests: `TestProjectAndSessionIsolation` ×5, `TestRegistryIdentityCaching` ×2, `TestBufferEviction` ×2, `TestRestartEquivalence` ×1, `TestResolveContextIdentity` ×3) | **Process note**: the architectural fix was implemented and verified passing (via the sibling test suites below) before this permanent test file was written. To still capture genuine before/after evidence, the fix files (`src/chuzom/context.py`, `src/chuzom/router.py`) were `git stash push`-ed with the new test file left in place, and the test run against the resulting pre-fix `context.py`: `ImportError: cannot import name '_buffers' from 'chuzom.context'` — collection-time failure, because the pre-fix module had only a single module-level `_session_buffer` singleton and a zero-arg `get_session_buffer()`; none of the registry primitives (`_buffers`, `_reset_session_buffers_for_test`, `_resolve_context_identity`, keyed `get_session_buffer(project_id, session_id)`) existed. This directly reproduces the B-04 root cause: one process-wide buffer shared by every project/session, so content recorded for one (project, session) leaked into `build_context_messages()` calls for any other. The fix was then restored via `git stash pop`. | 13/13 passed after `git stash pop` restored the fix (0.86s). Covers: A/B project switch with no cross-leak and independent persistence (`test_ab_project_switch_does_not_leak`), same-project/different-session isolation, same-session-id/different-project isolation, N concurrent (project, session) pairs each keeping unmixed history, `session_id=None` normalization not colliding with named sessions, identity-key caching (same key → same instance; falsy `project_id` normalized consistently), LRU-at-capacity eviction (`_MAX_BUFFERS`), idle-timeout eviction (`_BUFFER_IDLE_EVICT_SECONDS`) with content actually gone (not just re-created empty) after eviction, restart-equivalence via `_reset_session_buffers_for_test()`, and `_resolve_context_identity()`'s explicit-project-wins / session_store-fallback / fail-open-to-`"_unknown"` behavior. Also re-ran the full sibling/regression sweep (`tests/test_router*.py tests/test_context*.py tests/test_session_store*.py tests/test_context_capture_hook.py tests/test_session_context_wiring.py tests/test_session_end_context_archive.py tests/test_session_last_activity.py tests/test_session_start_context_pointer.py tests/test_sec01_shared_scrubber.py tests/test_red2_04_privacy_local_blocks_all_external.py tests/test_chz_aud_022.py tests/test_chz_aud_023.py tests/test_chz_aud_024.py tests/test_session_buffer_scope.py`): `293 passed, 1 warning` (the one warning is a pre-existing, unrelated `PytestUnraisableExceptionWarning` from asyncio subprocess `__del__` teardown in `tests/test_session_end_context_archive.py`, not caused by this change). | `src/chuzom/context.py` (registry: `_buffers: dict[tuple[str,str], SessionBuffer]`, `_MAX_BUFFERS=200`, `_BUFFER_IDLE_EVICT_SECONDS=6h`, LRU+idle eviction, `get_session_buffer(project_id, session_id=None)`, `_resolve_context_identity()`, `_reset_session_buffers_for_test()`; `build_context_messages()` rewired to resolve identity once and thread it through Layer 2/2b; `save_session_summary()`/`auto_summarize_session()` accept optional keyword-only `project_id`/`session_id`) · `src/chuzom/router.py` (success-path call site: consolidated the previously-independent `get_session_buffer()` zero-arg call and `session_store.resolve_session_id()` call into one shared `_resolve_context_identity(None, None)` resolution reused for both the in-process buffer and the durable session_store mirror) · `tests/test_context.py` (all 12 call sites updated to the keyed API via new `_ambient_identity()` test helper + `_reset_session_buffers_for_test()` fixture pattern — `TestBuildContextMessagesLayer2b`, `TestAutoSummarize`, `TestInjectedContextStripping`, plus 3 standalone tests) · `tests/test_chz_aud_023.py` (`_reset_buffer` fixture updated from poking the removed `_session_buffer` global to calling `_reset_session_buffers_for_test()`). Full-codebase sweep (`grep -rnE "get_session_buffer\(\s*\)"` across `src/` and `tests/`) confirms zero remaining zero-arg call sites outside documentation/comments. **Deferred, not yet done**: reusing this same `(identity)`-keyed registry pattern for `quality_feedback.py`'s `_quality_store`, as planned for Cluster 4's C-02 — intentionally out of scope for this commit. | `eb36301` |
| D-01 | 1 — Sensitive-content lifecycle | `tests/test_cluster1_persistence_hardening.py` (17 tests spanning three sections — `TestResultCacheRedaction`, `TestSemanticCacheRedaction`, `TestSessionStoreRedaction` — each planting synthetic secrets: an OpenAI-shaped `sk-...` key, an AWS `AKIA...` key, a GitHub `ghp_...` token, a JWT, a prose-phrased password ("the password is hunter2play9"), a prose-phrased "launch code is ORANGE-742", and an email address) | Ran the redaction-coverage tests against a version of `result_cache.py`/`semantic_cache.py`/`session_store.py` with the `persist_redact()` call sites removed (isolated via `git stash` of just this cluster's changes): every planted secret round-tripped verbatim — `assert SECRET_OPENAI_KEY not in raw_bytes` failed with the raw `sk-...` string present unredacted in the on-disk `.db` file bytes (both the main table row and the FTS5 `results_content`/`results_data` shadow-table blocks, since FTS5 external-content tables live in the same physical file), in `semantic_cache`'s shared `usage.db`, and in `session_store`'s JSONL line bytes — directly reproducing D-01 (secrets persisted unredacted, world-readable, FTS-searchable, cross-provider-replayable). | 17/17 passed (0.98s standalone). Every test opens the on-disk file directly (`Path(db_path).read_bytes()` for SQLite, line-by-line UTF-8 read for JSONL) and greps the raw bytes — never the query API — confirming each of the 7 synthetic secrets is absent post-store while a `[REDACTED:...]` marker is present in its place; the FTS5 shadow tables are covered for free because they share the same `.db` file as the main table, so one whole-file byte-grep proves both. `CHUZOM_PERSIST_RAW=1` opt-in-escape-hatch test confirms verbatim retention still works when explicitly requested (proving the gate, not just the default). | `tests/test_result_cache.py`, `tests/test_semantic_cache.py`, `tests/test_session_store.py`, `tests/test_session_store_concurrency.py`, `tests/test_st004_semantic_cache_project_scope.py` — full targeted sweep together with the 17 new tests: `99 passed`. One genuine regression found and fixed mid-cluster: `test_st004_semantic_cache_project_scope.py::test_same_project_still_hits` failed with `AssertionError: within-project semantic cache must still hit` because the D-02 perms-repair call sites in `semantic_cache.py` used direct `config.chuzom_db_path` attribute access, which raised `AttributeError` (silently swallowed by an existing broad `except Exception`) against that test's pre-existing minimal `_Cfg` config double (defines only `ollama_base_url`); fixed by hardening `_repair_shared_db_perms()` to accept `db_path: Path \| None` and no-op on `None`, and switching both call sites to `getattr(config, "chuzom_db_path", None)`, matching the defensive pattern already used by `persist_redact()` itself. Re-run after fix: `99 passed, 0 failed`. | `<pending>` |
| D-02 | 1 — Sensitive-content lifecycle | Shared with D-01's test file — perms assertions live in the same three `TestResultCacheRedaction` / `TestSemanticCacheRedaction` / `TestSessionStoreRedaction` sections (`test_*_creates_file_with_0600_perms`, `test_*_repairs_unsafe_perms_on_existing_file`) | Same isolated pre-fix run as D-01: newly created `result_cache.db`, `semantic_cache`'s shared `usage.db`, and `session_store`'s JSONL/db files inherited the process umask (world/group-readable, e.g. `0644`), matching `cost.py`'s previously-fixed reference pattern being absent from these three call sites — directly reproducing D-02. A file pre-created with mode `0644` and then opened via the store's normal open path retained `0644` (no repair-on-open behavior existed). | 17/17 passed. `result_cache.py`/`session_store.py` create new sensitive files via `Path.touch(mode=0o600)` and additionally repair pre-existing unsafe perms at open time via a `stat.S_IMODE(...) != 0o600` check + conditional `os.chmod`; `session_store.py` applies an unconditional `os.chmod(path, 0o600)` after every write for defense-in-depth. `semantic_cache.py` repairs perms on its shared `usage.db` via `_repair_shared_db_perms()` at the start of both `check()` and `store()`. Test asserts: (a) a freshly created file is `0600` immediately, (b) a file pre-created at `0644` is repaired to `0600` after the next store/check call. **Documented limitation** (not a gap, called out explicitly in the test and here): because `semantic_cache.py`'s shared `usage.db` may be created for the first time by `cost.py`'s own `_get_db()` (off-limits to this cluster) rather than by `semantic_cache.py`, perms repair on a brand-new shared file only lands on `semantic_cache`'s *next* `store()`/`check()` call, not synchronously at creation — the test reflects this by repairing on the second call rather than asserting an unsupported same-call guarantee. | Same sibling sweep as D-01: `99 passed` (includes the `_repair_shared_db_perms(None)` no-op regression fix described in D-01's row). | `<pending>` |
| D-04 | 1 — Sensitive-content lifecycle | `tests/test_cluster1_persistence_hardening.py` in full (all 17 tests, parametrized across the three persistence layers by section rather than by `pytest.mark.parametrize`, per file/module conventions already in use by the sibling suites) | No prior test file covered redaction/perms/TTL together across all three stores — `find tests/ -iname "*cluster1*"` / `grep -rl "persist_redact\|CHUZOM_PERSIST_TTL_DAYS" tests/` returned nothing before this cluster, confirming the coverage gap described by D-04's acceptance criteria. | The new file is now a permanent, always-run part of `tests/` (no special markers/opt-in required) and was included in every regression sweep run for D-01/D-02/B-02/B-03 above and below, so CI will fail on any future regression in any of the three stores' redaction, perms, or TTL behavior — satisfying D-04's acceptance criterion directly rather than as a side effect. | Same sibling sweep as D-01/D-02: `99 passed`, and the regression it caught (the `config.chuzom_db_path` `AttributeError` described in D-01's row) is itself first-hand evidence the added coverage does its job — it caught a real bug introduced mid-cluster before it reached the commit. | `<pending>` |
| B-02 | 1 — Sensitive-content lifecycle | `tests/test_cluster1_persistence_hardening.py::TestSessionStoreRedaction::test_session_store_ttl_physically_deletes_expired_record` (plus a same-class `test_session_store_ttl_survives_fresh_store_instance` for the restart-durability half of the acceptance criterion) | Ran against pre-fix `session_store.py` (isolated via `git stash` of this cluster's changes, `_maybe_compact` unchanged from before Cluster 1): a JSONL line written with a synthetic old timestamp (`ts` far beyond `CHUZOM_PERSIST_TTL_DAYS=30`) remained present, byte-for-byte, in the file after calling `_maybe_compact()` — `assert OLD_MARKER not in raw_jsonl_bytes` failed — because no TTL-driven deletion path existed at all; `_scrub_secrets()` narrowed only at write time and never expired anything. Directly reproduces B-02 (indefinite retention). | Passed. `session_store.py`'s `_maybe_compact()` now runs a TTL purge pass — using a cheap pre-check (`_first_record()`'s own `"ts"` field) before a full-file scan/rewrite — that physically drops any record older than `_persist_ttl_seconds()` (from `CHUZOM_PERSIST_TTL_DAYS`, default 30) during the compaction rewrite (`os.replace()` of the rewritten file), not a read-time filter; a fresh `session_store` instance opened against the on-disk file post-compaction still shows the record gone (`test_session_store_ttl_survives_fresh_store_instance`), confirming physical deletion rather than an in-memory-only exclusion. Raw-byte-grep of the post-compaction JSONL file confirms the old marker string is completely absent from the file, not merely unreachable via the read API. | Same sibling sweep as D-01 (`99 passed`), plus `tests/test_session_store_concurrency.py` specifically re-run standalone to confirm the new TTL purge pass inside `_maybe_compact()` does not reintroduce the C-01 torn-compaction race fixed under the file-lock critical section in the prior cluster commit (`0a87dd4`) — passed cleanly, no lost writes. | `<pending>` |
| B-03 | 1 — Sensitive-content lifecycle | `tests/test_cluster1_persistence_hardening.py` prose-secret assertions embedded in each of the three sections (not a standalone `secret_scrubber.py` unit test, since the fix is architectural — routing all three stores through the shared `persist_redact()` chain rather than patching `_scrub_secrets()`'s anchored regex in place) | Ran the prose-phrased secrets (`"the password is hunter2play9"`, `"the launch code is ORANGE-742"`) through pre-fix `session_store.record_event()` (isolated via `git stash`): both strings round-tripped verbatim into the JSONL file, since `secret_scrubber.py`'s `_SECRET_PATTERNS` are `[:=]`-anchored only (`key: value` / `key=value` forms) and have no coverage for unanchored "is"/"was" phrasing — directly reproducing B-03. | Passed. All three stores now call the shared `persist_redact()` (`enterprise/redaction.py`) before persisting, which layers `secret_scrubber.scrub_text()` (unchanged, still handles the anchored forms it always did) underneath a new `_redact_prose_secrets()` pass using `_PROSE_SECRET_RE` — a keyword-then-value regex covering `launch code`, `access code`, `passcode`, `password`, `pass phrase`, `secret (key)`, `api key`, `auth token`, `credential`, `pin code`, `security code` in `is`/`was`/`:`/`=` phrasing. Raw-byte-grep of the post-store JSONL/db files confirms both prose secrets are replaced with `[REDACTED:prose_secret]` and are byte-absent from the file. `secret_scrubber.py` itself was left unmodified (out of my file ownership) — the fix is additive, layered on top via `persist_redact()`, not a change to `_SECRET_PATTERNS`. | Same sibling sweep as D-01 (`99 passed`) — `secret_scrubber.py`'s own existing unit tests (implicitly exercised via `test_sec01_shared_scrubber.py`, not directly modified or re-run as part of this cluster's mandated sibling list, but no changes were made to that module so no regression risk exists there). | `<pending>` |
