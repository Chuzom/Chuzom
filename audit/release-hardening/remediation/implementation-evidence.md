# Release-Hardening Implementation Evidence

Compact before/after record per finding. Populated incrementally, one row-set
per cluster commit, in the dependency order defined by
`REMEDIATION_PLAN.md` / `REMEDIATION_MATRIX.json`.

| id | cluster | test added | before (fail reason) | after (pass) | sibling paths fixed | commit |
|----|---------|-----------|----------------------|--------------|----------------------|--------|
| C-01 | 6 — Concurrent persistence | `tests/test_session_store_concurrency.py::test_session_store_concurrency_zero_lost_writes` | Ran against pre-fix `session_store.py` (via `git stash`): `AssertionError: round 0: 26 writes were lost (first few: ['MARK-w0-i65-pid72752', 'MARK-w0-i91-pid72752', 'MARK-w0-i119-pid72752', 'MARK-w0-i120-pid72752', 'MARK-w0-i175-pid72752', 'MARK-w1-i60-pid72753', ...])` — 26/1200 (≈2.2%) lost writes across 6 real processes × 200 events, consistent in magnitude with the original audit's 22/1200 (1.83%) finding for CHZ-AUD-C-01. Root cause: `record_event()`'s append-then-maybe-compact critical section had no cross-process coordination — a concurrent process's compaction could read a stale file snapshot (taken before another process's append landed) and `os.replace()` the file with that stale snapshot, silently orphaning the just-written record. | Fixed via `src/chuzom/file_lock.py` (new cross-platform advisory exclusive lock: POSIX `fcntl.flock`, Windows `msvcrt.locking`, sibling `.lock` file so the data file's inode can be freely swapped by compaction's `os.replace()` without disturbing lock identity) wrapping the entire append+dedupe-check+maybe-compact critical section in `session_store.record_event()`. 0 lost writes across: the initial run (3 rounds × 6 workers × 200 events = 3600 writes), 3 additional standalone repeat runs (no flakiness), and the full adjacent/sibling sweep — `151 passed` across `tests/test_session_store.py`, `tests/test_session_store_concurrency.py`, `tests/test_context.py`, `tests/test_context_capture_hook.py`, `tests/test_session_context_wiring.py`, `tests/test_session_end_context_archive.py`, `tests/test_session_last_activity.py`, `tests/test_session_start_context_pointer.py`, `tests/test_sec01_shared_scrubber.py`, `tests/test_red2_04_privacy_local_blocks_all_external.py`, `tests/test_chz_aud_022.py`, `tests/test_chz_aud_023.py`, `tests/test_chz_aud_024.py`. | None found. Searched `src/chuzom` for the same shape (periodic full-file-rewrite-via-compaction combined with concurrent multi-process appends to the same file): `grep -rn "os.replace\|mkstemp" src/chuzom --include="*.py"` (13 files — all either SQLite-based (`cost.py`) or simple atomic single-file writes without a compaction-rewrite race) and `grep -rln "\.jsonl\|_maybe_compact\|compact_to\|COMPACT_TO" src/chuzom --include="*.py"` (~24 JSONL-referencing files) narrowed to the closest JSONL-log-like candidates — `lineage/lineage_store.py`, `library/sealer.py`, `hooks/lineage_integration.py`, `hooks/savings_logger.py`, `model_tracking.py` — none of which use `os.replace`/`mkstemp` (`grep -rn "os.replace\|mkstemp" <those 5 files>` → no output), i.e. none perform compaction-style full-file rewrites. The torn-compaction race pattern is unique to `session_store.py` in this codebase. | (pending — see below) |
