# Secrets
.env
.env.backup.*
.env.bak.*

# Python build/cache
__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/
.mypy_cache/
dist/
build/
*.egg-info/
.venv/
.coverage*
htmlcov/

# OS
.DS_Store
Thumbs.db

# Tessera local state (per-user)
.tessera_cache/
.tessera_lineage/
*.tessera.sqlite*
.serena/
.lore/

# Editor / IDE caches
.claude/tasks/
.claude/task-list.md
.tasks.json
.playwright-mcp/
.playwright-cli/

# LOCAL-ONLY DOCUMENTATION (NEVER COMMIT)
# Uppercase Docs/ = internal scratch workspace
# docs-private/ = enterprise-only documentation (hardened features, RBAC, audit chain, etc.)
Docs/
docs-private/
# Lowercase docs/ IS committed — public operator documentation only
!docs/
!docs/**
INTERNAL.md
STRATEGY.md
MONETIZATION.md
ROADMAP.md
PRICING.md
CLAUDE.md

# Test artifacts
.router-test-results/
test_results/
*.bak
*.bak.*

# Benchmark artifacts — cache is per-machine, results bloat over time.
# Commit specific results explicitly when they're evidence-worthy.
bench/cache/
bench/results/

# Audit working dir — evidence ledger, batch checkpoints, remediation plan
# (Per audit charter — read-only audit artifacts, never committed)
.audit/
