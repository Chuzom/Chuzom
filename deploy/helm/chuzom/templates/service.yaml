apiVersion: v1
kind: Service
metadata:
  name: {{ include "chuzom.fullname" . }}
  labels:
    {{- include "chuzom.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "chuzom.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
