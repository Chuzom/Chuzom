replicaCount: 1

image:
  repository: chuzom-router
  tag: "local"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 17891

# P1-4: liveness/readiness probes. The SSE entrypoint has no unauthenticated
# HTTP health route, so these are TCP probes on the listen port. For the admin
# entrypoint you can swap to an httpGet on /v1/admin/health in deployment.yaml.
probes:
  enabled: true
  liveness:
    initialDelaySeconds: 10
    periodSeconds: 20
    timeoutSeconds: 3
    failureThreshold: 3
  readiness:
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 3
    failureThreshold: 3

# Deployment posture. enterprise → RBAC strict, audit on, identity required.
deploymentProfile: enterprise
quotaMode: strict
sseAllowPublic: "on"
logJson: "1"

budget:
  backend: sqlite        # sqlite (single instance) | postgres (multi-instance)
  postgresDsn: ""        # required when backend=postgres

oidc:
  issuer: ""
  audience: ""
  roleMap: ""            # e.g. "chuzom-admins=admin,chuzom-users=employee"

# Secret values rendered into a Kubernetes Secret and injected as env. Prefer
# setting these via --set / an external secrets operator rather than committing.
secrets:
  OPENAI_API_KEY: ""
  ANTHROPIC_API_KEY: ""
  GEMINI_API_KEY: ""

ingress:
  enabled: false
  className: ""
  host: chuzom.example.com
  annotations: {}
  tls: false

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: "1"
    memory: 1Gi

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 10001
  fsGroup: 10001
